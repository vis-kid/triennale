#container {
  .project-headline {
    @extend .first-title;
    font-size: em(21);
    line-height: 1.7em;
  }
  .location{
    @extend .small-title;
    text-align: left;
    margin-bottom: 7px;
  }
}
